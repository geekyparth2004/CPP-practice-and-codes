#include<bits/stdc++.h>
using namespace std; 
int main(); 

string s = ""; 

for(char i = 'a' ; i < 'z' ; ++i)
{   
    for(char  j = 'a' ; j < 'z' ; ++j)
{
    cout<< i + 'a' + 'n' + 'g' + j;
}
       
}