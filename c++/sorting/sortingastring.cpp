#include<iostream>
#include<vector>
using namespace std;
int main()
{
    vector<string> s;
    vector<string> l;

    for(int i=0;i<s.size()<i++)
    {
        string m;
        cin>>m[i];
        s.push_back(m[i]);
    }

    for(int l=0;l.size();l++)
    {
        if(s[l]>='X')
        l.push_back(s[l]);
    }

   
}