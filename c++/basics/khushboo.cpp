#include<iostream>
#include<climits>
using namespace std; 
int main()
{
    int n; 
    cout<<"Enter the size of the array";
    cin>>n;

    int arr[n]; 

    for(int l = 0; l<=n-1 ; l++)
    {
        cin>>arr[l];
    }

    int max = INT_MIN;

    for(int i = 0 ; )
}